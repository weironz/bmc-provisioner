{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Desktop shell may hand HTTPS BMC links to the operating system's default browser.",
  "windows": ["main"],
  "permissions": ["core:default", "opener:default"]
}
